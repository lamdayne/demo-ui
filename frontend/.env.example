# Green Trace Frontend Environment Variables

# The base URL of your deployed Express backend API (e.g. on Render, Railway, Vercel Serverless)
# Do NOT end this path with a trailing slash.
VITE_API_BASE=http://localhost:5000/api
